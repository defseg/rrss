<li>
	<h2>
		<a href="<%= entry.link %>">
			<%= entry.title %>
		</a>
	</h2>
	<% if show_feed_titles %>
		<i><%= link_to entry.feed.title, feed_url(entry.feed) %></i><br>
	<% end %>
	<%= sanitize(entry.content_encoded) %>
</li>